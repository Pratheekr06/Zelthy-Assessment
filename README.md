## This Project is built without using any packages. 
It is built using HTML, CSS and Javascript, using React Framework.\
Run `npm install` to install dependencies.
`npm start` to run the application
